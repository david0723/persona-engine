{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && if ! git diff-index --quiet HEAD -- 2>/dev/null || [ -n \"$(git ls-files --others --exclude-standard)\" ]; then git add -A && git commit -m \"wip: auto-commit uncommitted changes\"; fi"
          }
        ]
      }
    ]
  }
}
